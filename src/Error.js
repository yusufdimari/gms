import React from "react";

export default function Error() {
  return (
    <div
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
        textAlign: "center",
        color: "white",
      }}
    >
      Error, Page not Available
    </div>
  );
}
